const num = [1,2,3,4];
let result = num.reduce(
    (cacculation, e) => {
        let optel = cacculation.sum += e; //is optelling
        let vermedig = cacculation.mult * e; // is vermenig vuldign
    }
)